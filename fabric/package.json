{
  "name": "fabric",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "asbuild:untouched": "asc index.ts -b build/untouched.wasm -t build/untouched.wat --use Math=JSMath --sourceMap --validate",
    "asbuild:optimized": "asc index.ts -b build/fabric.wasm -t build/optimized.wat --use Math=JSMath --sourceMap --validate --optimize --noDebug",
    "build": "npm run asbuild:untouched && npm run asbuild:optimized"
  },
  "author": "Gerald A. de Jong",
  "license": "GPL",
  "devDependencies": {
    "assemblyscript": "github:AssemblyScript/assemblyscript",
    "typescript": "^2.9.2"
  }
}
