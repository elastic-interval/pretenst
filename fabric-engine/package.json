{
  "name": "fabric-engine",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "asc index.ts -b build/fabric.wasm -t build/optimized.wat --sourceMap --validate --optimize",
    "prep-sym": "asc index.ts -b build/fabric-unoptimized.wasm -t build/unoptimized.wat --sourceMap --validate",
    "sym": "wasm-as build/unoptimized.wat -s symbols.txt -o build/fabric-unoptimized.wasm"
  },
  "author": "Gerald A. de Jong",
  "license": "",
  "devDependencies": {
    "assemblyscript": "github:AssemblyScript/assemblyscript",
    "typescript": "^2.9.2"
  }
}
